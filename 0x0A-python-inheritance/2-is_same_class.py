#!/usr/bin/python3
""" Define function is same class """


def is_same_class(obj, a_class):
    """ Is same class function. """
    if type(obj) == a_class:
        return True
    else:
        return False
